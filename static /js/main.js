$(document).ready(() => {
    // Click Events
    $('.hint-box').on('click', () => {
        $('.hint').slideToggle(800);
    });

    $('wrong-answer-one').on('click', () => {

    });

    $('wrong-answer-two').on('click', () => {

    });

    $('wrong-answer-three').on('click', () => {

    });
});